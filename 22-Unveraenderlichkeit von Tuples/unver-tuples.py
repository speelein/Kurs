# geordnete Sammlung von Elementen aehnlich von Listen
# koennen nicht geaendert werden ( unusible )

from re import M


mein_tuble = (1, 2, 3) # 0 1 2 Index ( fest )
print(mein_tuble)
# mit und ohne Klammern moeglich

ohne_klammern = 1, 2, 3
print(ohne_klammern)
ein_Element = (1)
print(ein_Element)